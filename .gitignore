/vendor/
/composer.lock
/.phpunit.cache/
/.phpunit.result.cache
/phpunit.xml
/.php-cs-fixer.cache
/build/
/coverage/

# composer.lock is deliberately NOT committed: this is a LIBRARY, so the lock
# would only pin the dev toolchain and would fight every consumer's own
# resolution. Applications commit theirs; libraries do not.
