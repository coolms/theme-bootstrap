<div{loop:config.attr} {var:item.key}="{var:item.value}"{endloop}>
    {var:message raw}
    {if:dismissible}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    {endif}
</div>
